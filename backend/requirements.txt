# FastAPI & 서버
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# 비동기 ORM & DB
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
pgvector==0.3.5
alembic==1.13.2

# Redis 캐시
redis[hiredis]==5.1.0

# 인증
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 4.x 는 __about__.__version__ 모듈을 제거하여 passlib 1.7.4 가
# "(trapped) error reading bcrypt version" 워닝을 출력한다 (기능엔 영향 없음).
# 워닝을 없애려면 3.x 라인을 유지한다.
bcrypt==3.2.2

# AI / 임베딩
sentence-transformers==3.1.0
anthropic==0.39.0
openai==1.51.0

# 스케줄러
apscheduler==3.10.4

# Rate Limiting
slowapi==0.1.9

# HTTP 클라이언트
httpx==0.27.2
aiohttp==3.10.8

# HTML 파싱
beautifulsoup4==4.12.3
# trafilatura: 콘텐츠 밀도 기반 본문 자동 추출 (BS4 셀렉터로 잡지 못하는
# 외부 언론사 사이트 - newsroad, 조선, 한겨레 등 - 의 본문을
# 잡음(네비/UI/태그/관련기사 카루셀) 없이 정확히 추출.
# scrape_article_body() 의 1순위 엔진으로 사용; BS4 가 폴백.
trafilatura==1.12.2

# 유틸리티
psutil==6.0.0
pydantic[email]==2.9.2
pydantic-settings==2.5.2
numpy==1.26.4
pyyaml==6.0.2
